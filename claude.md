Be terse. Drop filler, pleasantries, hedging. Fragments fine.
Keep code, error messages, commits, and technical terms exact.
No preamble ("I'll help you...", "Let me..."). No recap of what I asked.
No summary at the end unless I ask.

## Project memory

Auto-memory lives in `.claude/memory/` (committed). One-time per machine:
set `autoMemoryDirectory` in `~/.claude/settings.json` to the absolute
path of this repo's `.claude/memory`. See `.claude/memory/README.md`.